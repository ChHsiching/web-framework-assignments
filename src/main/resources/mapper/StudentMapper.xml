<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="点击召唤下节课可能会讲的 mapper 接口，闭眼以忽略红色波浪线">
    <select id="selectStudent" resultType="hashmap">
        SELECT * FROM student
    </select>
</mapper>